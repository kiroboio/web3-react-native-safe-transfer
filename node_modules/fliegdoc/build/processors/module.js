"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.processModule = void 0;
const init_1 = require("./init");
const processJsDocs_1 = require("./helpers/processJsDocs");
/**
 * Converts a `NamespaceDeclaration` to a documentation-ready representation.
 *
 * @param node - the namespace or module declaration
 * @returns documentation-ready representation of the namespace or module
 * @example
 * ```ts
 * processModule(node);
 * ```
 */
function processModule(node) {
    if (node.hasNamespaceKeyword()) {
        const res = {
            name: node.getName(),
            docs: processJsDocs_1.processJsDocs(node.getJsDocs()),
            type: 'namespace',
            declarations: []
        };
        for (const [, declarations] of node.getExportedDeclarations()) {
            res.declarations.push(...declarations.map(init_1.processNode));
        }
        return res;
    }
    // TODO: Module type
    return { type: 'module', name: node.getName(), declarations: [] };
}
exports.processModule = processModule;
//# sourceMappingURL=module.js.map